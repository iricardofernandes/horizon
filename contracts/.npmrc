# Publishing target. CI overrides this with its own registry host; the protocol is
# identical, so no module configuration changes between local and CI (ADR 0029).
@horizon:registry=http://localhost:4873
